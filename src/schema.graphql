type Query {
    vehicles: [Vehicle!]!
    owners: [Owner!]!
}

type Vehicle {
    id: ID!
    brand: Brand!
    type: String!
    plate: String!
    owner: Owner!
}

type Owner {
    id: ID!
    name: String!
    vehicles: [Vehicle!]!
}

type Brand {
    id: ID!
    name: String!
}